element* generate(char board[][],int color)
{
	int* moves[6][32];
	init(moves);
	element *head={.nastepny=NULL,.ruch={.poz1=0,.poz2=0}};
	for (int i = 0; i < 8; i++)
	{
		for (int j = 0; j < 8; j++)
		{

		}
	}
}
